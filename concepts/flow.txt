게임 시작

block_pick
// 카드 충전 애니메이션

선택 시
	block_load
	// 선택 애니메이션 *
	선택 해제 시
		block_unload
		// 선택 해제 애니메이션 *
	
	(이동, 회전)
	block_turn_right
	block_turn_left
	block_flip_front
	block_flip_side
	block_move_up
	block_move_down
	block_move_right
	block_move_left
	block_move_to
	// 버튼 클릭 애니메이션 *
	// 이동, 회전 애니메이션 *

	배치 시도 시
		block_drop_check
		// 배치 애니메이션 *
		block_drop

		block_delete_check
		// 삭제 애니메이션, 점수 애니메이션 *
		block_delete

		// 턴 상승 애니메이션 *
		block_pick
		// 카드 충전 애니메이션 *
		
goto 선택 시;

항상
	// 베이스 UI 출력 *
	// 돌 상태 출력 *
	// 점수, 턴 출력 *
	// 카드 위 가이드 출력 *
	// 버튼 (상태) 출력